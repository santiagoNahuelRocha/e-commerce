<%- include('../template/head') %>
<%- include('../template/navAdmin') %>
<div class="container d-flex justify-content-center text-center">
    <form action="/admin/editar"  method="post"  enctype="multipart/form-data" style="width:50%; background: #eaeaea;" class="mt-2 px-5 py-2 ">
        <% producto.forEach((producto) => { %>
            <form action="/admin/editar"  method="post">
                <input type="hidden" name="id" id="id" value="<%= producto.id %>">
                <div class="form-group my-3">
                    <input type="text" class="form-control" name="title" id="title"
                        value="<%= producto.title %>">
                </div>
                <div class="form-group my-3">
                    <textarea class="form-control" name="description" id="description" cols="30"
                        rows="10" ><%= producto.description %></textarea>
                </div>
                <div class="form-group my-3">
                    <input type="number" class="form-control" name="price" id="price"
                        value="<%= producto.price %>">
                </div>
                <div class="form-group my-3">
                    <input type="file" class="form-control" name="image" id="image">
                </div>
                <button type="submit" class="btn btn-success">Editar</button>
            </form>
        <% }); %>
    </form>
</div>





<%- include('../template/footer') %>




<%- include('../template/cierre') %>